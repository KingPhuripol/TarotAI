// ─── API Configuration ────────────────────────────────────────────────────────
// The OpenAI API key is now managed server-side via Vercel Environment Variables.
// This file is intentionally kept as an empty stub — safe to commit.
// See README for local development instructions.

const OPENAI_API_KEY = ""; // key lives in Vercel dashboard, not here
